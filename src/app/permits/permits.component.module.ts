import { NgModule } from '@angular/core';
import { IonicModule } from 'ionic-angular';
import { PermitsComponent } from './permits.component';

@NgModule({
  declarations: [
    PermitsComponent,
  ],
  imports: [
    PermitsComponent,
  ],
  exports: [
    PermitsComponent
  ]
})
export class PermitsComponentModule {}
